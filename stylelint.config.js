export default {
	extends: ["stylelint-config-standard"],
	rules: {
		// Allow flexible naming patterns (project uses custom conventions)
		"custom-property-pattern": null,
		"selector-class-pattern": null,
		"keyframes-name-pattern": null,
	},
};
